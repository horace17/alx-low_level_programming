#include "main.h"
/**
 * main - main function
 * Return: End program
 */

int main(void)
{
	const char[] = "_putchar\n";
	int len = strlen(str);
	int i;

	for (i = 0; i < len; i++)
	_putchar(str[i]);
	return (0);
}
